spring:
  application:
    name: content-service
  cloud:
    config:
      uri: http://${config.hostname:config}:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
